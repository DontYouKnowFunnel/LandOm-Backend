spring:
  profiles:
    active: local
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  threads:
    virtual:
      enabled: true

clickhouse:
  host: ${CLICKHOUSE_HOST}
  port: ${CLICKHOUSE_PORT}
  database: ${CLICKHOUSE_DATABASE}
  username: ${CLICKHOUSE_USER}
  password: ${CLICKHOUSE_PASSWORD}
  schema-path: classpath:init-db/init-clickhouse.sql

jwt:
  secret:
    key: ${JWT_SECRET_KEY}

server:
  url: ${SERVER_URL}

frontend:
  url: ${FRONTEND_URL}

llm:
  server:
    url: ${LLM_SERVER_URL}
