spring:
  # 1. Database (MySQL) ??
  datasource:
    # Docker Compose ??? ??? 'db'? ???? ?????.
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://db:3306/myapp_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    username: ${SPRING_DATASOURCE_USERNAME:root}
    password: ${SPRING_DATASOURCE_PASSWORD:root}
    driver-class-name: com.mysql.cj.jdbc.Driver

  # 2. JPA/Hibernate ??
  jpa:
    hibernate:
      ddl-auto: update # MVP ?? ????? ?? ?? ?? ??
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  # 3. Java 21 ?? ??? ??? (?? ???)
  threads:
    virtual:
      enabled: true

# 4. ??? ClickHouse ??? ??
clickhouse:
  host: ${CLICKHOUSE_HOST:clickhouse}
  port: 8123
  database: default